# 三组课程真实计时与协作验证（2026-09-07—08）

当前固定配置的B组已跑完，C运行中，A待接续；OCR三组均值尚未产生。下面包含真实失败和质量限制，不能把实际消耗时间当作三组均成功、准确转写的时间。

## 样例与动态筛选

维护者已更正：原图是一千多张，按每小时24–40张动态选择，50–80不是所有时长的固定上限。三组是同一课程相邻常规讲次，时长相差不到两分钟；每组八个均匀时点目视为正对黑板及教师的正常机位。所有原始帧时间戳按manifest逐项核验为8秒间隔。

| 组 | 课程时长 | 原始截图 | 自然选中 | 张/小时 | 重启后筛选合计 |
|---|---:|---:|---:|---:|---:|
| A | 161.293 min | 1210 | 75 | 27.90 | 37.953311 s |
| B | 163.246 min | 1225 | 97 | 35.65 | 43.963594 s |
| C | 163.135 min | 1224 | 96 | 35.31 | 51.695071 s |
| 平均 | — | — | — | — | **44.537325 s** |

这是现有内部`select_video_frame_candidates()`的真实扫描、解码/缩略图、选择与复制计时，由`tools/benchmark_course_frame_selection.py`桥接；当前没有新的公开“预抽帧目录去重”API或CLI。核心原有28–40张/小时目标未变，三组实际都在维护者24–40范围内。曾提出的80张测试截取在任何正式OCR前撤销；全部75/97/96张都进入计划，不事后删除空板来改变成绩。

输入已经完成FFmpeg抽帧，表中不含视频解码/抽帧。重建三组的扫描/选择/复制分别为A 36.589361/0.248014/1.115936秒，B 42.672566/0.243665/1.047363秒，C 49.850922/0.223374/1.620775秒。

断电前同三包的另一轮实测为14.143923/13.906490/14.031804秒，平均14.027406秒。两次未控制文件缓存，恢复时还重新安装了环境依赖，不能把差异单独归因为缓存、BIOS或硬件故障，也不能把14秒当作固定保证。

原始媒体只读，识别正文和原始记录留在本机；附件`R:\archieve\AGENTS.md`用作样例格式背景，不把其中历史迁移指令当成本轮请求。本轮没有建设下载、迁移存储或未来完整CLI工作流。

## OCR：固定16K总上下文、8K输出预算

GPU为RTX3090 24GiB、驱动610.88。服务模型ID为`qwen3.8-27b-q6-k-medium-ocr`，GGUF元数据的架构为Qwen3.5 hybrid；二者不能混作同一种命名。使用维护者选择的Model Lab既往`ocrllm-legacy`模板，包含逐帧标记和Mermaid/SVG/SMILES转写，**medium思考**。公共库对应独立`course_ocr` / `course.legacy.v1`；原`detail_ocr`的`board.v17`保持不变。

| 组 | 选中图/计划批次 | 整组wall | HTTP调用 | 最终技术状态 |
|---|---:|---:|---:|---|
| A | 75 / 38 | 待接续 | — | 未完成计时 |
| B | 97 / 49 | **5797.015423 s（96分37秒）** | 49 | partial；46批/92张settled，末3批/5张失败 |
| C | 96 / 48 | 运行中 | — | 未完成计时 |
| 平均 | — | **未计算** | — | 必须等三组首轮终结 |

B的49次请求均HTTP200、stop，没有HTTP错误、自动重试或length截断；HTTP累计5789.947854秒。输出tokens中三批超过旧4096上限，分别4133/4186/5302。源hash不变；已发布92个frame marker与对应settled输入顺序逐项匹配，但全97张完整性不通过。

B末5张已目视为空板/擦板，无可读课程板书，5/5源大小及hash与state一致。末3次正文字符数94/94/46，与仅返回帧标记相容；**原始失败正文未保存，不能声称已读到逐字原文**。现有校验会去除HTML注释，再要求可见字母、数字或符号，marker-only会被判`PROVIDER_RESPONSE_INVALID/no recognition Markdown`。因此更倾向于空课程内容与当前非空正文契约冲突，不能归因为上下文不足，也不据此放行所有空回复或改写partial。

格式抽查：旧A五批均有裸SVG；新B部分结果抽五批，帧标记10/10对应正确，其中一批的两个SVG仍缺少要求的svg围栏。目视抽样未证明明显整帧空缺或完全无关内容，但不证明字符/公式准确率。HTTP200或settled不等于定制输出约束全部满足。

## ASR：转码、物理切片、识别与一次恢复

正式ASR及恢复均使用库当前`audio.long.interval.v1`提示词。每课17个10分钟逻辑片段，`include_boundary_context=False`；没有拿无提示词诊断替代正式成绩。

| 组 | 输入转码 | 首轮wall（含切片） | 一次恢复wall | 合计 | 调用/最终状态 |
|---|---:|---:|---:|---:|---|
| A | 20.354 s | 366.902818 s | 0 | **387.256818 s（6分27秒）** | 17；complete，4个NOSPEECH需质量核验 |
| B | 20.379 s | 390.920593 s | 11.341455 s | **422.641048 s（7分3秒）** | 18；partial，1片段失败，3个NOSPEECH |
| C | 20.363 s | 773.394067 s | 320.926698 s | **1114.683764 s（18分35秒）** | 20；partial，1片段失败，2个NOSPEECH |
| 平均 | — | — | — | **641.527210 s（10分42秒）** | **实际消耗平均，不是全成功平均** |

首轮物理切片累计A/B/C为14.416933/15.068291/15.168664秒，已包含在wall中，不能再加一次。B恢复复用16片、仅再调用失败片1次；C复用16片、失败片再调用2次（含有限自动重试）。失败复发后停止，没有无限恢复。B为invalid no-speech marker；C为HTTP502 response_incomplete，不能用独立OCR后端的上下文日志解释ASR错误。仅首轮含输入转码的平均530.771秒同样不是全成功平均。

**提示词确实发送了，但兼容性存在实证问题。** 服务将附加text作为Qwen ASR context/hotwords，不能假定能执行完整窗口指令。代码及输入特征核查没有发现10分钟音频被默认截成30秒。A的一段30秒同bytes、16kHz诊断，exact prompt与无text均返回讲话转写；完整10分钟同bytes诊断则为：

| 独立诊断请求 | 时间 | 输出字符/tokens | 结果 |
|---|---:|---:|---|
| exact库提示词 | 1.675191 s | 15 / 4 | NOSPEECH4OCRLLM |
| 省略text | 22.980217 s | 1759 / 1171 | 非sentinel转写 |

这证明提示词会影响同一音频的结果，不能把快速NOSPEECH当作可靠整段无语音，也不能认定1759字逐字正确。诊断不计入正式表，未改运行时提示词。详见[AAC/MP3调查](aac_mp3_sample_rate_investigation_2026-09-07.md)和[ASR CPU调查](asr_cpu_investigation_2026-09-07.md)。

原始音频是24kHz单声道AAC/M4A。库当前入口只接受MP3，先转为24kHz、64kbps MP3；随后`materialize_long_audio_interval.py`明确以`-ar 16000 -ac 1 -b:a 64k -c:a libmp3lame`物理切片，实际提交16kHz，Model Lab也按16kHz归一化。AAC本身不必降低采样率，这次下降来自明确切片参数；两次有损编码不等于已测出质量损失幅度或NOSPEECH的原因。

ASR实际缓存为`Qwen/Qwen3-ASR-1.7B-hf`固定revision，当前BF16、cuda:0。按要求由Terra/max调查CPU可行性：旧参考目录性能都是0.6B，不能套到此1.7B-hf；精确模型CPU速度、RSS及导出未实测。官方OVMS存在CPU常驻服务，但其multipart `/v3/audio/transcriptions`当前不支持prompt，也不是本库JSON Chat input_audio+text的直接替换。CPU执行可减少显存竞争，但不能保证不争CPU/内存/I/O；本轮未启动CPU模型干扰计时。

## 计时、冷加载与断电证据

`tools/benchmark_course_recognition.py`通过公共batchify/recognize和split/recognize流程，包装真实HTTP及subprocess边界计时，不替换库内部函数或模拟正式模型响应。图片batch2，单通道，请求上限600秒；rate-limited/unavailable/timeout各允许一次额外重试、等待2秒。计入wall的是本次规划、快照、识别、切片、重试和写输出；工具额外前后hash审计及最后summary JSON写入不在wall内。HTTP/FFmpeg是内部明细，不重复相加。`passed`只表示技术complete且源hash不变。

模型冷加载/独立预热不计入表中暖运行时间。ASR独立30秒讲话预热为22.175403秒（含首次加载），不计入三课平均。恢复后的同模板两图8K预热为508.542131秒；16K/8K已知难例probe为587.282203秒，含约327秒加载，输入处理5.15379秒、生成252.34071秒，2676输入+4643生成=7319，stop、truncated=0、1call。它不是整课耗时或暖批速度。断电前另一次预热342.9665秒用了近似legacy模板，不与exact模板预热混作同样本对照。

维护者报告误触电源后恢复。旧进程和/tmp数据已消失，提交57638f3保住旧A与ASR汇总，但临时原始正文/事件/checkpoint丢失，不能伪称从不存在的checkpoint恢复。新证据改放home持久目录；B/C/A使用同一新16K/8K配置重新完整计时。旧B中断观察不是完整计时，旧C未跑。ASR表是断电前真实工具结果及已提交记录的历史汇总；私人ledger明确标记来源，不冒充重启后的新调用。

旧8K总context/4K生成配置的A：75图38批，45调用，31批61张settled、7批14张失败，wall5097.921420秒（84分58秒），HTTP5078.545385秒，partial。7个失败slot为9/24/26/28/30/34/36，各有限重试一次。旧OCR共16次502（含中断B的一对）均映射到4096生成tokens；2676输入+4096生成，release n_tokens6771、truncated=0，证据指向独立输出上限，不是8192输入context溢出。不能将旧A与新B/C拼成同配置平均。

## 两个仓库改动与严重性

- OCRLLM `feca47c`补齐安全provider code/request ID在checkpoint中的诊断，504正确归类timeout。有限重试和断点恢复早已存在；原缺陷妨碍定位与按timeout策略恢复，没有证据证明整个库不可用或已发生数据丢失。
- Model Lab `3ca2805`、`33c632a`修复进程/端口/health身份、保留活着的UNREADY进程、精确归属清理与restart。真实出现的“端口在、manager却DISABLED”能阻断无人值守恢复，是严重生产障碍。预算和队列等待改为校验后的配置，永久context错误返回413；ASR合作式时限结合EOS检查，不将提前截断当stop。上一轮54项库测试、15项服务测试及真实媒体/故障恢复有记录，不能当作吞吐或准确率证明。
- 本轮`course_ocr`原样迁入维护者选择的既有模板，并按原文件名渲染，无运行时跨repo import；16项相关测试及真实HTTP边界的retry/resume/state-loss repair验证通过，既有wheel门槛和轻量import通过。
- Model Lab `04030e5`将明确`finish_reason=length`在解析正文前分类为422/output_token_limit，覆盖null/空/部分正文，避免原参数当临时502重试。按需边界场景及11项服务契约测试通过，真实难例通过证明扩预算可解决该实例，不能冒充真实触发422测试。
- 新发现的HTTP200后本地正文校验失败未携带request ID，已在隔离副本准备图片/音频诊断修复。54项相关测试、合成真实HTTP场景及351985B wheel通过；**active代码尚未应用**，等固定三组结束后核验落地。它不放宽空正文或sentinel规则，不新增checkpoint schema。

两仓继续只通过HTTP合作：Model Lab负责模型、预算、生命周期；OCRLLM负责选择、分批、重试、恢复。没有跨仓私有import、张量批处理加速或CPU常驻注册已完成的虚假声明。

## 上下文优化与显卡调查

Luna/max按精确混合架构调查：q8_0 full-attention KV估算8K约272MiB、12K约408MiB、16K约544MiB；8K到16K增加约272MiB不是整机峰值或最终设备放置证明。当前16K/8K有单批与B整组证据，但生成曾由约30降至18.4tok/s，原因不能单靠显存快照归为CPU offload。

同图同prompt的8K生成断电前后为1744tokens、30.02/30.05tok/s，不支持“断电后GPU整体变慢”的简单归因。8K/5500与12K/8192是尚未执行的紧凑预算候选；前者对2676输入仅余16token总空间，不能推广到更长输入。候选必须固定图/prompt/medium、分开冷加载，并保留完整结束及输出规模。 当前16K/8K来自本次启动环境覆盖，源码默认仍为8K/4K；尚未把新值设为持久默认，重启时必须明确沿用本次预算，不能假定普通新启动自动继承。

运行中只读遥测显示当前功耗上限350W、PCIe Gen4 x16、无热/硬件供电降频；约304W是功耗而非电压。BIOS两项名称仍未知。Windows日志更像按键触发的系统关机；唯一WHEA为无法归属设备的启动期信息级厂商CPER。不能以此保证硬件绝无问题。全部任务结束后的复查尚待执行，详见[功耗、BIOS与上下文跟进](gpu_power_and_context_followup_2026-09-08.md)。
