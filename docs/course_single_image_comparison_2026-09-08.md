# 单图第四组：8K/7K与无提示词ASR（实测完成，部分识别失败）

维护者本轮决定：默认先单图一批；OCR只检查完整帧HTML注释，包括对应原始文件名，不审核正文或图形格式；模型任务规则放在Model Lab；ASR直接输入音频、不带提示词。最初root将完整注释误解为任意注释，已撤回该课程校验改动，未将错误规则用于正式第四组。

## 职责与实现

本次第四组使用的Model Lab持久HTTP服务识别既有legacy课程提示词首行及帧注释指令，只验证返回文本中是否包含本批各原始文件名对应的完整`<!-- meta:frame id=原文件名 -->`注释，不要求独占一行，不检查重复、额外标记或排列；缺失/错名返回HTTP422 `course_frame_markers_invalid`。只审注释，不添加或修复模型标记；返回内容仍经过manager既有的首尾空白strip，并非逐字节原样。普通OCR请求不强加课程规则。同一持久服务的ASR公共入口兼容接收text但不交给模型，manager调用processor时明确prompt=None。旧一次性诊断adapter仍存在补marker和terminology_prompt路径，本次不调用，不能把持久服务结论扩大到旧诊断路径。

OCRLLM只新增通用provider选项：`response_validation="nonempty_text"`让服务负责图片内容语义，库保留字符串/编码/非空与传输完整性检查；`send_audio_prompt=False`使请求只含input_audio。默认其他provider仍用原markdown验证和音频prompt。没有在库里添加帧名解析、ModelLab URL分支或课程专用校验。OCRLLM仍保留既有course_ocr profile及提示词渲染；本轮模型任务校验放在ModelLab，不把既有profile的存在描述为已迁出。默认单图落实于本机测试工具和ModelLab既有provider默认；其他云provider批次未改。

merged resume原本允许更换provider并重用settled；新选项同样仅影响未完成slot，不新增state schema。为避免比较时混入旧prompt结果，本次使用全新输出目录，未复用前三组结果。

## 配置可行性与先行功耗观测

RTX3090，8,192总context/7,168最大输出，medium，图像上限1,024tokens，原legacy提示词不删减，不缩图。首图实测1,649输入+346生成、HTTP200/stop；冷请求149.879417秒，其中模型输入2.20405秒、生成11.23972秒（30.69tokens/s），其余主要含加载，不能当暖推理时间。

因此配置能够启动、短输出能完成，但不能保证生成满7K：8192−1649=6543，且还需终止余量。没有把“max output”冒充预留空间。服务源码默认已随本轮试验切为8K/7K，环境覆盖仍保留。旧16K/8K实验记录仍是原条件。

在正式组之外，连续5张热观测全部HTTP200/stop，输入均1649tokens：

| 选中序号（零基） | 输出tokens | HTTP秒 |
|---|---:|---:|
| 40 |1562|53.452650|
| 41 |1932|65.731829|
| 42 |1766|60.577638|
| 43 |2781|94.777883|
| 44 |2117|72.605840|

观察到瞬时功耗最高346.65W、一秒平均最高341.27W、GPU利用率99%、温度78°C、显存23034MiB；未见热或硬件供电降频。该快照统计来自功耗观测段，不冒充整组峰值。290/350对应功耗W；当前WSL驱动`nvidia-smi -q -d VOLTAGE`没有返回电压数值，不能声称测得最高电压。高功耗不等同于性能或健康证明，但已排除本段“始终被限制在300W以下”的描述。

## 第四组口径

复用原B同一门163.246分钟正常机位课程、1225原图、97选中图（35.65张/小时），从而控制输入；不是另挑一门课程来混淆模型配置比较。另行复测筛选：扫描7.897443秒、选择0.110308秒、复制0.352801秒，合计8.360552秒；97个选中时间戳与旧B逐项一致，原图只读。与旧B43.963594秒未控制缓存，不能把这部分差异算作模型优化收益。正式单图97批，对照旧B双图49批、16K/8K。提示词仍legacy+medium，模型服务顺序使用GPU。切片10分钟，不重叠，ASR使用同课音频，单独暖机后计时；转码仍24kHz MP3，物理切片仍16kHz，与旧B保持一致。

正式序列及一次有界图片恢复已结束，以下保留真实partial结果与全部失败耗时。首轮前段使用过额外检查标记数量/顺序的版本，出现两次422；没有保留失败模型原文，不能将其归因于模型错名。已在保全在途响应后部署仅检查存在性的最终规则，10:52:28—10:53:36维护暂停及之后冷加载均包含在正式耗时中。这一轮不是无中断的纯暖机控制实验；初轮结束后已仅续跑未完成项一次，单独记录耗时。不能把先行5图外推为97图完成时间；也不能把空板由失败变通过造成的状态变化当作内容准确率提升。

## 已完成的第四组OCR

97张首轮5648.444448秒（94分08秒）：95 settled，两个slot（零基20/22）被服务以422/course_frame_markers_invalid拒绝。最终存在性规则下仅续跑一次，130.628757秒，复用95 settled，仅新增2次请求；同两张仍缺少要求的准确完整注释，因此最终95/97、partial。没有补marker伪造成功，也没有继续循环重试。续跑失败证明最终存在性要求仍未满足；首轮失败原文未留存，不能据此反推首轮究竟是缺失、错名或额外数量限制。

| 同课对照 | 旧B：双图，16K/8K | D：单图，8K/7K |
|---|---:|---:|
| 输入/选中 |1225/97|1225/97|
| OCR wall，含本组续跑 |5797.015423秒（96分37秒）|5779.073204秒（96分19秒）|
| HTTP调用 |49|99（首轮97+续跑2）|
| 结果 |92/97，partial|95/97，partial|
| HTTP200生成tokens |117355|154270|
| HTTP错误 |0|4次422，涉及同2张|
| 成功HTTP的finish_reason |全部stop|全部stop|

两组端到端OCR只差约18秒（0.31%），不能声称单图明显提速。D的HTTP200生成量更多，4次拒绝的生成量未在HTTP错误中返回，不能把154270当作包括失败在内的全部计算量。D包含前述维护/冷加载，且验收规则改变，既不是纯参数隔离实验，也不证明准确率提升。没有出现502/length失败。来源指纹前后相同。

wall按benchmark首轮+续跑相加；父进程记录的image process_wall包含等待root启动续跑、父暂停及续跑等待，不能再与resume wall叠加。独立warmup、额外5图功耗观察和外层hash审计不计入此表。

## 第四组ASR：无提示词没有稳定通过长切片

同课音频原长9794.774秒，转码MP3的库规划长度9794.795333秒（编码时长差约0.021秒）；17个10分钟、不重叠切片，最后一片较短。30秒独立暖机37.648305秒（含加载）已排除。正式请求20次全部只有1个input_audio、0个text；服务manager同样固定prompt=None。

| 同课对照 | 旧B：带提示词 | D：无提示词 |
|---|---:|---:|
| 输入转码 |20.379秒|20.970039秒|
| 正式ASR wall（含物理切片和自动重试） |390.920593秒|1441.441436秒|
| 额外手动恢复 |11.341455秒|0|
| 转码+切片+ASR合计 |422.641048秒（7分03秒）|1462.411475秒（24分22秒）|
| HTTP调用 |18|20|
| 最终未完成切片 |1|3（零基5/15/16）|
| 库判定NOSPEECH |3|0|

D物理切片14.463837秒已经包含在wall中，不再叠加。14/17 settled，3片各两次502/response_incomplete（现有策略的一次自动重试），六次失败HTTP合计987.199271秒，约16分27秒。所有HTTP200均stop；原音频指纹不变。无手动ASR恢复、不改回旧提示词，最终partial。没有正文准确率/无语音真实性审核；0个NOSPEECH不代表所有片段真的有语音。旧B断电前历史结果与D当前实测的环境/缓存未严格隔离，不能将全部时差量化归因于prompt，但本轮不能支持“无提示词长片稳定且更快”的说法。

这一现象属于ASR，不用独立OCR后端的8K上下文解释。ASR仍为Qwen3-ASR-1.7B、BF16 GPU；本轮未运行CPU模型。此前CPU调查中的“无prompt协议差异”因维护者取消ASR提示词而不再是功能要求障碍，CPU速度/内存与接口适配仍未实测。

## 运行后修正与显卡复查

本轮实测服务为69f3954。代码复核发现ASR的已知output-token-limit分支仍发502，而且未EOS分支先执行；通用OpenAI 5xx分类因此可能自动重试相同参数。运行结束后在ModelLab将达到ASR_MAX_OUTPUT_TOKENS的检查前置为422/output_token_limit，沿用OCR已有错误语义，OCRLLM无厂商特判；额外日志只记input/output token数量、耗时和EOS，不记音频或转写正文。未达上限但缺EOS的其他情形仍按原timeout/incomplete处理。旧实测未留这些内部计数，不能断言六次错误均由上限引起；新分支修正不追溯改写本组结果，也不宣称3个失败转写已被修复。

GPU两段采样共3900行，10:20:06—12:32:17，间隔约2秒，计时器交接有95秒采样缺口。包含暖机、5图观察、正式OCR/续跑和ASR，不能当作连续无遗漏监测。记录瞬时峰值364.66W、驱动平均功耗峰值343.49W、最高78°C、100%利用率、23040MiB显存；未采到thermal/power-brake Active。12:32任务后只读快照：P8、0%利用率、36°C、13.86W。Windows从本次开机以来仅有开机时WHEA ID3/Information；未见本轮新增Display/nvlddmkm错误、Kernel-Power41或EventLog6008。未取得电压数值，不能把W叫作V，也不能以此保证硬件绝对无损。没有发现本轮GPU异常证据。

本轮私有审计保存在`/home/model-lab/course-benchmark-20260908-fourth`：formal初轮/恢复JSON、events/checkpoint、final-audit、维护时刻、GPU采样及最终事件JSON。源媒体不发布。两仓只经HTTP合作，没有跨仓运行时import。

## 当前验证

库相关image/provider25项、audio/provider39项通过；通用nonempty_text模式本地真实SDK/HTTP场景证明metadata-only能通过并从空回复失败恢复，旧markdown默认仍拒绝空/注释-only。ModelLab最初14项服务契约测试通过；收尾加入ASR cap有/无EOS及正常完成的模型和音频解码边界替身案例后15项通过，无GPU调用。覆盖正确注释-only、缺失/错名及普通OCR隔离、ASR忽略text。无额外自动审核SVG/公式/正文。包新增两个通用选项后wheel352360B，较上轮增加375B；门槛从344KiB调整为345KiB，轻量import与相关gate检查23passed/3平台skip。

运行后修正提交为ModelLab `efd21fc`，已在所有实测结束后通过既有manager重启部署，健康检查RUNNING（PID110061）。未再加载模型做额外压力测试。帧名存在性规则仍为既有69f3954版本的行为；本次重启没有改OCR提示词、单图/medium或8K/7K预算。
